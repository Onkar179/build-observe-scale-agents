# Demos

Demo code from the session.

| Demo | Description |
|---|---|
| [`finops-cloud-agent`](finops-cloud-agent) | FinBot, a GCP FinOps agent built with ADK and connected to BigQuery billing data through MCP Toolbox. |

The demo code is intentionally minimal and public-safe. It focuses on the core agent, prompt, MCP integration and configuration examples rather than a full production deployment scaffold.
